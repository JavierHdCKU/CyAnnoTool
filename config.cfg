[DEFAULT]
# 1. Path to the executable script relative to the module's repository root
SCRIPT=run_cyanno.py 

# 2. Defines the type of execution (usually 'python' for Python scripts)
COMMAND_TYPE=python

# 3. Defines the input arguments (mapping to the input files)
INPUT_FILES=matrix_file, labels_file

# 4. Defines the positional arguments for the command line
POSITIONAL_ARGUMENTS=matrix_file, labels_file, prediction_cyanno_file

# 5. Defines the output arguments (matching the names used in your YAML outputs)
OUTPUT_FILES=prediction_cyanno_file